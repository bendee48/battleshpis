/**
  * Represents a player.
  * @constructor
  * @param {string} type - Signifies whether player is human or AI
**/
class Player {
  constructor(type) {
    this.type = type;
  }
}

export default Player;